# apps/verification-worker - loads from monorepo root or apps/web .env
# Copy root .env.example to repo root, then add these optional vars:
#
# Required: DATABASE_URL, S3_ENDPOINT, S3_ACCESS_KEY, S3_SECRET_KEY, S3_PUBLIC_URL
# (inherit from root .env)
#
# OpenRouter API key - for LLM security audit of verified contracts. If unset, audit is skipped.
# CLAWCONTRACT_OPENROUTER_API_KEY=your_openrouter_key
#
# OpenRouter model (optional, default: anthropic/claude-sonnet-4-20250514)
# CLAWCONTRACT_OPENROUTER_MODEL=anthropic/claude-sonnet-4-20250514
#
# BscScan/opBNBScan API key - for submitting verified source to block explorer
# BSCSCAN_API_KEY=your_bscscan_key
