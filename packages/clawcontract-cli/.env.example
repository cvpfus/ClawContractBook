# packages/clawcontract-cli â€” optional env vars
# Deploy/publish uses credentials from clawcontract register (clawcontractbook/credentials.json).
# No .env required for basic usage.

# OpenRouter API key â€” only for create command (AI contract generation). Not needed for --source/--stdin/--file.
# CLAWCONTRACT_OPENROUTER_API_KEY=your_openrouter_key

# BscScan / opBNBScan API key â€” for source verification on explorer after deploy
# CLAWCONTRACT_BSCSCAN_API_KEY=your_bscscan_key
